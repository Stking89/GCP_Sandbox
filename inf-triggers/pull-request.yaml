steps:

  - name: 'gcr.io/cloud-builders/docker'
    args:
     - pull
     - hashicorp/terraform:latest  

  - name: 'Init'
    id: terraform-init
    entrypoint: terraform
    args: ['init']

  - name: 'Plan'
    id: terraform-plan
    entrypoint: terraform
    args: ['plan']