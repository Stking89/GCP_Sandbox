steps:

  - name: 'gcr.io/cloud-builders/docker'
    args:
     - pull
     - hashicorp/terraform:latest  

  - name: 'terraform'
    id: terraform-init
    entrypoint: terraform
    args: ['init']

  - name: 'terraform'
    id: terraform-plan
    entrypoint: terraform
    args: ['plan']